<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.player32611.mapper.SetmealDishMapper">
    <select id="selectSetmealIdByDishIds" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish where dish_id in
        <foreach collection="dishIds" item="dishId" separator="," open="(" close=")">
            #{dishId}
        </foreach>
    </select>

    <insert id="insertBatch">
        insert into setmeal_dish (setmeal_id, dish_id, name, price, copies) VALUES
        <foreach collection="setmealDishes" item="sd" separator=",">
            (#{sd.setmealId}, #{sd.dishId}, #{sd.name}, #{sd.price}, #{sd.copies})
        </foreach>
    </insert>

    <delete id="deleteBySetmealIds">
        delete from setmeal_dish where setmeal_id in
        <foreach collection="setmealIds" open="(" close=")" separator="," item="dishId">
            #{dishId}
        </foreach>
    </delete>

    <select id="selectSetmealDishVOBySetmealId"
            parameterType="java.lang.Long"
            resultType="com.player32611.vo.SetmealDishVO">
        SELECT
        d.name,
        d.description,
        d.image,
        sd.copies
        FROM setmeal_dish sd
        LEFT JOIN dish d ON sd.dish_id = d.id
        WHERE sd.setmeal_id = #{id}
    </select>
</mapper>