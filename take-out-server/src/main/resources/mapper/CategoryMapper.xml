<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.player32611.mapper.CategoryMapper">
    <select id="selectByNameLikeAndType" resultType="com.player32611.entity.Category">
        SELECT
        id,
        name,
        type,
        sort,
        status,
        create_time,
        update_time,
        create_user,
        update_user
        FROM category
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>

            <if test="type != null">
                AND type = #{type}
            </if>
        </where>
        order by sort
    </select>

    <update id="update">
        update category
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="sort != null">
                sort = #{sort},
            </if>
            <if test="status != null">
                status = #{status},
            </if>

            update_time = #{updateTime},
            update_user = #{updateUser}
        </set>
        where id = #{id}
    </update>
</mapper>